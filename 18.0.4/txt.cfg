default autoinstall

label  auto-install
  menu label ^Auto Install default server (first installation) Ubuntu Server From Preseed
  kernel /install/vmlinuz
  append  boot=casper file=/cdrom/preseed/auto-inst.seed vga=788 initrd=/install/initrd.gz auto=true priority=critical locale=fr_FR  quiet 
